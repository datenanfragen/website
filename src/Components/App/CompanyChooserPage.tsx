import { SetPageFunction } from './App';

type CompanyChooserPageProps = {
    setPage: SetPageFunction;
};

export const CompanyChooserPage = (props: CompanyChooserPageProps) => {
    return <></>;
};
